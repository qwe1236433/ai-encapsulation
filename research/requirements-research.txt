# Offline research baseline (optional). Not merged into api/requirements.txt.
# pip install -r research/requirements-research.txt
# File must stay ASCII-only so pip on Windows (GBK) can parse it.

numpy>=1.24
pandas>=2.0
scikit-learn>=1.3
